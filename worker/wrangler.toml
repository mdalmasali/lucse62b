name            = "lucse62b-api"
main            = "worker.js"
compatibility_date = "2024-01-01"

[vars]
# Non-secret env vars (if any) go here.
# Secrets (SUPA_KEY, DRIVE_API_KEY, etc.) are stored in Cloudflare dashboard
# under Workers → lucse62b-api → Settings → Variables → Secrets.
CPG_TEACHERS_GID = "1051481415"   # gid of the CPG_Teachers tab (from sheet URL ?gid=)

[[kv_namespaces]]
binding  = "SMS_RATE"
id       = "58bc2d2c8f2f45f78e5802fab91f09b4"

[ai]
binding = "AI"
